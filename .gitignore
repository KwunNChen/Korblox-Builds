# Rojo build output
*.rbxl
*.rbxlx
*.rbxl.lock
*.rbxlx.lock

# Toolchain binaries -- rokit installs itself to ~/.rokit/bin, so a copy
# sitting in the repo is redundant and should never be committed.
rokit.exe

# OS noise
Thumbs.db
.DS_Store

# Extracted reference material. Adonis, the TREK framework and the Vehicles of
# Requsition plugin are other people's code -- kept locally for checking
# compatibility against, never published. Regenerate it from the place file if
# you need it on another machine.
TREK Voicelines/reference/
